package Sel1605;

import com.paulhammant.ngwebdriver.ByAngular;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

import java.time.Duration;

public class Class5 {
    public static void main(String[] args) throws InterruptedException {
        // אנגולר Angular
/*        WebDriver driver = new ChromeDriver();

        driver.get("https://myhealth.stanfordhealthcare.org/#/");
//        driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(10)); //לא עובד
        Thread.sleep(3000);

        driver.findElement(ByAngular.model("id")).sendKeys("user");
        driver.findElement(ByAngular.model("password")).sendKeys("password");
        driver.findElement(ByAngular.buttonText("Sign In")).click();

 */

        // ג'יי יוניט גרסא 4 Junit
        

    }
}
